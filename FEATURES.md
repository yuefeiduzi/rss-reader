# 功能清单

每个功能都标注**真实实现状态**。历史版本的本文件曾把若干未实现的功能写成已完成，本次已逐条核对源码。

图例：✅ 已实现且有测试/实测 ｜ 🟡 已实现未验证 ｜ ⛔ 代码存在但从未接线（死代码） ｜ ❌ 未实现

---

## 一、订阅源管理

| 状态 | 功能 | 说明 |
|---|---|---|
| ✅ | 添加 RSS/Atom 订阅源 | 支持裸域名输入，自动补 `https://`；非 http/https 的 scheme 会被拒绝 |
| ✅ | 删除订阅源 | 确认后删除，级联删除关联文章 |
| 🟡 | 置顶订阅源 | 置顶的排在列表最前 |
|  | 按添加时间排序 | 新的在前 |
| 🟡 | 未读文章计数 | 每个订阅源显示未读数 |
| 🟡 | 编辑自定义名称 | 显示名优先用自定义名称 |
| ❌ | 预设热门订阅源 | **不存在**（旧文档声称有 Hacker News/Dribbble/The Verge 等，实际从未实现） |
| 🟡 | 交互方式 | 点击更多按钮 / 长按 / 右键菜单（桌面端） |

## 二、文章列表

| 状态 | 功能 | 说明 |
|---|---|---|
| 🟡 | 卡片式布局 | 标题、摘要、头图、作者 |
| ✅ | 未读/已读标记 | 未读数由 `StorageService` 增量维护，不再每次遍历全部文章 |
| ✅ | 收藏 | 点击爱心切换；状态取自仓库实时副本 |
| ✅ | 下拉刷新 | |
| ✅ | 强制刷新 | 清空该源缓存后重新抓取 |
| ✅ | 纯文本摘要提取 | 从 HTML 提取约 120 字 |
| ✅ | 相对时间显示 | X分钟前 / X小时前 / X天前，超过一周显示日期 |
| ✅ | 不再过度刷新 | 只在打开订阅源时拉一次；打开文章、切换收藏不再触发整源刷新 |
| ❌ | 图片懒加载 | **未实现**（旧文档声称有），列表用的是 `Image.network` |

## 三、文章详情

| 状态 | 功能 | 说明 |
|---|---|---|
| ✅ | 全文抓取 | 摘要过短时抓取原文正文；已修掉曾经必然返回空串的 bug |
| ✅ | HTML 渲染 | `flutter_widget_from_html_core`（已从停更的 `flutter_html` 迁出） |
| ✅ | 相对图片补全 | 按文章链接把 `/images/a.jpg` 补成绝对地址；识别 `data-src` 懒加载 |
| ✅ | 缓存优先读取 | 优先展示已缓存的全文，避免重复抓取 |
| ✅ | 收藏切换 | 状态取自仓库实时副本 |
|  | 外部浏览器打开 | 正文里的链接点击也走这里 |
|  | 分享 | |
| ✅ | 图片全屏画廊（缩放/平移） | 点击正文图片打开；面板早已写好但长期未接线 |
|  | 单图预览 `_showImagePreview` | 约 100 行，已被多图画廊取代，仍无调用者 |
|  | 图片右键菜单 | `_showImageContextMenu` 约 80 行，同样从未接线 |
| ⛔ | 渲染异常降级为纯文本 | `_hasError` 只读不写，降级分支永远不会进入 |

## 四、主题

| 状态 | 功能 | 说明 |
|---|---|---|
| ✅ | 浅色 / 暗色 / 跟随系统 | 已修掉「跟随系统」重启后失效的 bug |
| 🟡 | 首页快捷切换 | |
|  | Material 3 | |

## 五、响应式布局

| 状态 | 功能 | 说明 |
|---|---|---|
| ✅ | 窄屏单页导航 | 单一标题栏 + 返回箭头；已修掉曾经的双层标题栏 |
| ✅ | 宽屏分栏 | 左侧订阅源（宽度可拖），右侧列表/详情并列 |
| ✅ | 两种布局共用同一套内容区 | 不再各写一遍分支 |
|  | 其余页面宽屏适配 | `ResponsiveLayout` 只在首页用到 |

## 六、数据存储与缓存

| 状态 | 功能 | 说明 |
|---|---|---|
|  | SharedPreferences 持久化 | feeds / articles / config |
| ✅ | 全文内容缓存 | 7 天过期 |
| ✅ | 缓存过期判断 | |
| ⛔ | 图片缓存（Base64） | `cacheImage` / `getCachedImage` **无调用者** |
|  | 缓存统计 | `getCacheStats` 无调用者 |
|  | 30 天过期文章自动清理 | `clearOldArticles` **无调用者**，不会自动执行 |

## 七、备份恢复

| 状态 | 功能 | 说明 |
|---|---|---|
| 🟡 | JSON 完整备份 / 恢复 | zip 内含 backup.json + subscriptions.opml |
| ✅ | OPML 导出 | |
| ✅ | OPML 导入 | 已重写解析器，不再依赖属性顺序；修复前自己导出的 OPML 自己都导不回来 |
| ✅ | 导入去重 | 按 url 去重，返回值是真正新增的数量 |
|  | 备份文件列表 | `getBackupFiles` 已修正路径与扩展名匹配，但仍无 UI 调用 |
| ❌ | Web 端备份 | `dart:io` 在 Web 抛 `UnsupportedError` |

## 八、RSS/Atom 解析

| 状态 | 功能 | 说明 |
|---|---|---|
| ✅ | RSS 2.0 解析 | |
| ✅ | Atom 1.0 解析 | 需按 `published`(String) 与 `updated`(DateTime) 的不同类型分别处理 |
| ✅ | `pubDate` 日期 | |
| ✅ | `dc:date` 日期回退 | 已实现。仅用 `dc:date` 的源过去会全部退化成「刚刚」 |
| ✅ | `dc:creator` 作者 | |
| ✅ | 头图提取（含懒加载与相对路径） | |
| ✅ | 按发布时间倒序 | |
| 🟡 | RSS 1.0 (RDF) | 未实测 |
| ✅ | 解析逻辑可单测 | 抽出 `parseFeed` / `parseArticles` 纯函数，见 `test/rss_service_test.dart` |

## 九、全文抓取

| 状态 | 功能 | 说明 |
|---|---|---|
| ✅ | 智能内容提取 | `article` / `.post-content` / `.entry-content` / `main` 等选择器 |
| ✅ | 广告与导航过滤 | 移除 script / style / nav / footer / header |
| ✅ | 失败时保留原文内容 | 抓取失败不再用空串覆盖正文 |

---

## 测试覆盖

`flutter test`：见 `test/` 目录。纯逻辑集中在 `lib/utils/`，均有单测：

- `feed_url_test.dart` — URL 规范化
- `opml_test.dart` — OPML 解析与 XML 实体转义
- `date_format_test.dart` — 日期格式化与时区
- `html_content_test.dart` — 正文图片提取与补全
- `rss_service_test.dart` — RSS/Atom 解析、`dc:date`、日期回退

`widget_test.dart` 目前只断言标题，属占位性质。